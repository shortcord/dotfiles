# This file has been auto-generated by i3-config-wizard(1).
# It will not be overwritten, so edit it as you like.
#
# Should you change your keyboard layout some time, delete
# this file and re-run i3-config-wizard(1).
#

# i3 config file (v4)
#
# Please see https://i3wm.org/docs/userguide.html for a complete reference!

set $mod Mod4

# Font for window titles. Will also be used by the bar unless a different font
# is used in the bar {} block below.
font pango:monospace 8

# This font is widely installed, provides lots of unicode glyphs, right-to-left
# text rendering and scalability on retina/hidpi displays (thanks to pango).
font pango:DejaVu Sans Mono 8

# Start XDG autostart .desktop files using dex. See also
# https://wiki.archlinux.org/index.php/XDG_Autostart
exec --no-startup-id dex --autostart --environment i3

# Set compose key to caps; caps is a useless key anyway
# exec --no-startup-id setxkbmap -option compose:caps

# Prevent windows from stealing focus
focus_on_window_activation none

# Use wpctl to adjust volume.
set $refresh_i3status killall -SIGUSR1 i3status
bindsym XF86AudioRaiseVolume exec --no-startup-id wpctl set-volume @DEFAULT_AUDIO_SINK@ 2%+ --limit=1.0 && $refresh_i3status
bindsym XF86AudioLowerVolume exec --no-startup-id wpctl set-volume @DEFAULT_AUDIO_SINK@ 2%- --limit=1.0 && $refresh_i3status
bindsym XF86AudioMute exec --no-startup-id wpctl set-mute @DEFAULT_AUDIO_SINK@ toggle && $refresh_i3status

bindsym XF86AudioStop exec --no-startup-id playerctl pause
bindsym XF86AudioPlay exec --no-startup-id playerctl play-pause
bindsym XF86AudioNext exec --no-startup-id playerctl next
bindsym XF86AudioPrev exec --no-startup-id playerctl previous

# Use Mouse+$mod to drag floating windows to their wanted position
floating_modifier $mod

# move tiling windows via drag & drop by left-clicking into the title bar,
# or left-clicking anywhere into the window while holding the floating modifier.
tiling_drag modifier titlebar

bindsym $mod+l exec betterlockscreen --lock dim --off 15

# start a terminal
bindsym $mod+c exec alacritty
bindsym $mod+e exec --no-startup-id xfe
bindsym $mod+f exec nixGL firefox

## Screenshots
bindsym Print exec --no-startup-id maim | xclip -selection clipboard -t image/png
bindsym Shift+Print exec --no-startup-id maim --window $(xdotool getactivewindow) | xclip -selection clipboard -t image/png
bindsym $mod+Print exec --no-startup-id maim --select | xclip -selection clipboard -t image/png

# kill focused window
bindsym $mod+q kill
# bindsym $mod+Shift+F12 exec --no-startup-id xkill

# start rofi (a program launcher)
bindsym $mod+space exec --no-startup-id rofi -show drun -mode drun  -theme Arc-Dark -font 'hack 10' -show-icons

# change focus
bindsym $mod+Left focus left
bindsym $mod+Down focus down
bindsym $mod+Up focus up
bindsym $mod+Right focus right

# move focused window
bindsym $mod+Shift+Left move left
bindsym $mod+Shift+Down move down
bindsym $mod+Shift+Up move up
bindsym $mod+Shift+Right move right

# toggle tiling / floating
bindsym $mod+v floating toggle

# enter fullscreen mode for the focused container
bindsym $mod+Shift+f fullscreen toggle

# focus the parent container
bindsym $mod+a focus parent

# Define names for default workspaces for which we configure key bindings later on.
# We use variables to avoid repeating the names in multiple places.

# Left Monitor
set $wsl1 "1"
set $wsl2 "2"
set $wsl3 "3"
set $wsl4 "4"
set $wsl5 "5"
set $wsl6 "6"
set $wsl7 "7"
set $wsl8 "8"
set $wsl9 "9"
set $wsl10 "10"
set $wsl11 "11"
set $wsl12 "12"

# Right Monitor
set $wsr1 "13"
set $wsr2 "14"
set $wsr3 "15"
set $wsr4 "16"
set $wsr5 "17"
set $wsr6 "18"
set $wsr7 "19"
set $wsr8 "20"
set $wsr9 "21"
set $wsr10 "22"
set $wsr11 "23"
set $wsr12 "24"


# I seem to hate my fingers
# lock workspaces to their monitors
# Left Monitor
workspace $wsl1 output DisplayPort-1
workspace $wsl2 output DisplayPort-1
workspace $wsl3 output DisplayPort-1
workspace $wsl4 output DisplayPort-1
workspace $wsl5 output DisplayPort-1
workspace $wsl6 output DisplayPort-1
workspace $wsl7 output DisplayPort-1
workspace $wsl8 output DisplayPort-1
workspace $wsl9 output DisplayPort-1
workspace $wsl10 output DisplayPort-1
workspace $wsl11 output DisplayPort-1
workspace $wsl12 output DisplayPort-1

# Right Monitor
workspace $wsr1 output DisplayPort-2
workspace $wsr2 output DisplayPort-2
workspace $wsr3 output DisplayPort-2
workspace $wsr4 output DisplayPort-2
workspace $wsr5 output DisplayPort-2
workspace $wsr6 output DisplayPort-2
workspace $wsr7 output DisplayPort-2
workspace $wsr8 output DisplayPort-2
workspace $wsr9 output DisplayPort-2
workspace $wsr10 output DisplayPort-2
workspace $wsr11 output DisplayPort-2
workspace $wsr12 output DisplayPort-2

# switch to workspace
# Left
bindsym $mod+F1 workspace number $wsl1
bindsym $mod+F2 workspace number $wsl2
bindsym $mod+F3 workspace number $wsl3
bindsym $mod+F4 workspace number $wsl4
bindsym $mod+F5 workspace number $wsl5
bindsym $mod+F6 workspace number $wsl6
bindsym $mod+F7 workspace number $wsl7
bindsym $mod+F8 workspace number $wsl8
bindsym $mod+F9 workspace number $wsl9
bindsym $mod+F10 workspace number $wsl10
bindsym $mod+F11 workspace number $wsl11
bindsym $mod+F12 workspace number $wsl12

# Right
bindsym $mod+mod1+F1 workspace number $wsr1
bindsym $mod+mod1+F2 workspace number $wsr2
bindsym $mod+mod1+F3 workspace number $wsr3
bindsym $mod+mod1+F4 workspace number $wsr4
bindsym $mod+mod1+F5 workspace number $wsr5
bindsym $mod+mod1+F6 workspace number $wsr6
bindsym $mod+mod1+F7 workspace number $wsr7
bindsym $mod+mod1+F8 workspace number $wsr8
bindsym $mod+mod1+F9 workspace number $wsr9
bindsym $mod+mod1+F10 workspace number $wsr10
bindsym $mod+mod1+F11 workspace number $wsr11
bindsym $mod+mod1+F12 workspace number $wsr12


# move focused container to workspace
# Left
bindsym $mod+Shift+F1 move container to workspace number $wsl1
bindsym $mod+Shift+F2 move container to workspace number $wsl2
bindsym $mod+Shift+F3 move container to workspace number $wsl3
bindsym $mod+Shift+F4 move container to workspace number $wsl4
bindsym $mod+Shift+F5 move container to workspace number $wsl5
bindsym $mod+Shift+F6 move container to workspace number $wsl6
bindsym $mod+Shift+F7 move container to workspace number $wsl7
bindsym $mod+Shift+F8 move container to workspace number $wsl8
bindsym $mod+Shift+F9 move container to workspace number $wsl9
bindsym $mod+Shift+F10 move container to workspace number $wsl10
bindsym $mod+Shift+F11 move container to workspace number $wsl11
bindsym $mod+Shift+F12 move container to workspace number $wsl12

# Right
bindsym $mod+Shift+mod1+F1 move container to workspace number $wsr1
bindsym $mod+Shift+mod1+F2 move container to workspace number $wsr2
bindsym $mod+Shift+mod1+F3 move container to workspace number $wsr3
bindsym $mod+Shift+mod1+F4 move container to workspace number $wsr4
bindsym $mod+Shift+mod1+F5 move container to workspace number $wsr5
bindsym $mod+Shift+mod1+F6 move container to workspace number $wsr6
bindsym $mod+Shift+mod1+F7 move container to workspace number $wsr7
bindsym $mod+Shift+mod1+F8 move container to workspace number $wsr8
bindsym $mod+Shift+mod1+F9 move container to workspace number $wsr9
bindsym $mod+Shift+mod1+F10 move container to workspace number $wsr10
bindsym $mod+Shift+mod1+F11 move container to workspace number $wsr11
bindsym $mod+Shift+mod1+F12 move container to workspace number $wsr12

# reload the configuration file
bindsym $mod+Shift+c reload
# restart i3 inplace (preserves your layout/session, can be used to upgrade i3)
bindsym $mod+Shift+r restart
# exit i3 (logs you out of your X session)
bindsym $mod+Shift+e exec "i3-nagbar -t warning -m 'You pressed the exit shortcut. Do you really want to exit i3? This will end your X session.' -B 'Yes, exit i3' 'i3-msg exit'"

# resize window (you can also use the mouse for that)
mode "resize" {
        # These bindings trigger as soon as you enter the resize mode

        # Pressing left will shrink the window’s width.
        # Pressing right will grow the window’s width.
        # Pressing up will shrink the window’s height.
        # Pressing down will grow the window’s height.
        bindsym Left resize shrink width 10 px or 10 ppt
        bindsym Down resize grow height 10 px or 10 ppt
        bindsym Up resize shrink height 10 px or 10 ppt
        bindsym Right resize grow width 10 px or 10 ppt

        # back to normal: Enter or Escape or $mod+r
        bindsym Return mode "default"
        bindsym Escape mode "default"
        bindsym $mod+r mode "default"
}

bindsym $mod+r mode "resize"

# Start i3bar to display a workspace bar (plus the system information i3status
# finds out, if available)
bar {
        position top
        status_command i3status
        tray_output DisplayPort-1
}

# Window Border
default_border pixel
smart_borders on

focus_follows_mouse yes

exec --no-startup-id feh --bg-fill ~/Pictures/cherry_blossom_by_tomprante-d8shzqg.jpg
exec --no-startup-id betterlockscreen --update ~/Pictures/cherry_blossom_by_tomprante-d8shzqg.jpg
